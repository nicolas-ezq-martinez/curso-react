import React from 'react';

const Tratamientos = ({ show }) => {
    return show && (
        <div className="marcas">
            <div>Tratamientos</div>
        </div>
    );
};

export default Tratamientos;
